name: CI-Stable-RDP

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Configure Core RDP Settings
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
        netsh advfirewall firewall add rule name="RDP-Port" dir=in action=allow protocol=TCP localport=3389
        Restart-Service -Name TermService -Force

    - name: Create RDP User
      run: |
        $Password = ConvertTo-SecureString "P@ssw0rd123!" -AsPlainText -Force
        New-LocalUser -Name "RemoteUser" -Password $Password -Description "RDP User" -AccountNeverExpires
        Add-LocalGroupMember -Group "Administrators" -Member "RemoteUser"
        Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RemoteUser"

    - name: Setup Playit.gg (Highly Stable)
      run: |
        # Download the Playit agent
        Invoke-WebRequest -Uri "https://github.com/playit-cloud/playit-agent/releases/latest/download/playit-windows-amd64.exe" -OutFile "playit.exe"
        
        # Start playit in the background
        # Note: On first run, it will provide a CLAIM URL in the logs. 
        # You click it once to link it to a free account, and then it stays permanent.
        Start-Process -FilePath ".\playit.exe" -ArgumentList "--secret ${{ secrets.PLAYIT_SECRET }}" -RedirectStandardOutput "playit_log.txt"
        
        Start-Sleep -s 15
        Get-Content playit_log.txt | Select-String "claim" || Get-Content playit_log.txt
        
    - name: Keep Alive
      run: Start-Sleep -s 21600
      
